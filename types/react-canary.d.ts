/**
 * React's <ViewTransition> ships in the React build Next bundles for the App
 * Router, and its types live in the canary entry of @types/react. This makes
 * them visible to the compiler; it adds nothing at runtime.
 */
/// <reference types="react/canary" />
